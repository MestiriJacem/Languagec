#include <stdio.h>

// Le préprocesseur exécute des instructions avant la compilation.
// Les directives de préprocesseur commencent toujours par un '#'.
// Exemple simple : inclusion d'un fichier d'en-tête standard.

int main() {
    printf("Hello, Préprocesseur C!\n");
    return 0;
}
